"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.DisjointSetArray = void 0;
    function DisjointSetArray(size) {
        this.parent = [];
        this.size = 0;
        this.size = size || 0;
        for (var i = 0; i < this.size; i++) {
            this.parent.push(i);
        }
    }
    DisjointSetArray.prototype.makeSet = function () {
        this.parent.push(this.size);
        this.size++;
        return this.size - 1;
    };
    DisjointSetArray.prototype.find = function (x) {
        if (this.parent[x] !== x) {
            this.parent[x] = this.find(this.parent[x]);
            return this.parent[x];
        }
        else {
            return x;
        }
    };
    DisjointSetArray.prototype.union = function (x, y) {
        x = this.find(x);
        y = this.find(y);
        if (x === y)
            return;
        this.parent[y] = x;
    };
    return DisjointSetArray;
}());
exports.DisjointSetArray = DisjointSetArray;
